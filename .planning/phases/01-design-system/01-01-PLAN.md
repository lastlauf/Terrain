---
phase: 01-design-system
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/index.css
  - index.html
autonomous: true
requirements:
  - DS-01
  - DS-02
  - DS-03
  - DS-05

must_haves:
  truths:
    - "body has background-color #F5F2ED — no dark backgrounds remain in base styles"
    - "Inter is the only font family used — Daydream font-face declaration is removed"
    - "All CSS custom properties use the new warm palette — no legacy dark hex values in :root"
    - "Utility classes (.btn-retro, .glass-panel, .input-retro) use the new design system tokens"
    - "No scanline overlay, no glow-shadow, no gradient background utilities remain in CSS"
    - "Spacing custom properties cover 4, 8, 12, 16, 24, 32, 48, 64, 96 — unchanged"
  artifacts:
    - path: "src/index.css"
      provides: "Complete design system tokens and component utility classes"
      contains: "--bg-base: #F5F2ED"
    - path: "index.html"
      provides: "HTML shell with Inter font load and correct theme-color"
      contains: "Inter"
  key_links:
    - from: "src/index.css :root"
      to: "all JSX files"
      via: "CSS custom properties"
      pattern: "var\\(--bg-base\\)"
    - from: "index.html"
      to: "browser font rendering"
      via: "Google Fonts link tag"
      pattern: "Inter"
---

<objective>
Replace the entire dark retro-pixel design token foundation with the TE x Rams warm neutral system. This plan touches only CSS and the HTML shell — no JSX component logic changes. All downstream plans depend on these tokens being correct.

Purpose: Every component in the app reads color, type, and spacing from CSS custom properties. Fixing the :root tokens here propagates the retheme to every consumer instantly.
Output: src/index.css with the new warm palette, type system, utility classes, and zero decorative artifacts. index.html updated with Inter font and correct theme-color.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace CSS design tokens — palette, type, and shadows</name>

  <read_first>
    - src/index.css (full file — understand every current token and utility before touching)
    - index.html (understand current font imports and theme-color)
  </read_first>

  <files>src/index.css, index.html</files>

  <action>
**index.html changes:**
- Change `<meta name="theme-color" content="#0D0A06" />` to `<meta name="theme-color" content="#F5F2ED" />`
- Remove the Boogaloo and Fredoka One font families from the Google Fonts link — keep Inter and add weights 400;500;600;700;800
- Remove Open Sans from the Google Fonts link entirely
- Final font link: `https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap`

**src/index.css — :root token replacements (exact values, line by line):**

REMOVE the @font-face block for Daydream entirely (lines 6–12).

Replace the entire :root block with this exact content:

```css
:root {
  /* Color Palette — TE x Rams warm neutral */
  --bg-base: #F5F2ED;
  --bg-surface: #EFECE6;
  --bg-surface-raised: #FFFFFF;
  --bg-muted: #E8E4DC;
  --border-light: #DDD8CE;
  --border-mid: #C8C2B5;
  --border-dark: #A89E8E;
  --accent-orange: #E8712B;
  --accent-orange-dark: #C45A1A;
  --accent-orange-bg: rgba(232, 113, 43, 0.08);
  --text-primary: #1A1714;
  --text-secondary: #4A4540;
  --text-muted: #7A7268;
  --text-dim: #A89E8E;
  --region-mountains: #4A6FA5;
  --region-forest: #4A8C5C;
  --region-city: #C45A1A;
  --region-coast: #3A7D9E;
  --danger: #D93B20;

  /* Spacing Scale (4px base) */
  --space-1: 4px;
  --space-2: 8px;
  --space-3: 12px;
  --space-4: 16px;
  --space-6: 24px;
  --space-8: 32px;
  --space-12: 48px;
  --space-16: 64px;
  --space-24: 96px;

  /* Type Scale */
  --text-xs: 12px;
  --text-sm: 14px;
  --text-base: 16px;
  --text-lg: 20px;
  --text-xl: 24px;
  --text-2xl: 32px;
  --text-3xl: 48px;
  --text-4xl: 64px;
  --text-5xl: 96px;

  /* Fonts — Inter only */
  --font-display: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  --font-heading: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  --font-body: 'Inter', -apple-system, BlinkMacSystemFont, sans-serif;
  --font-mono: 'Courier New', 'Courier', monospace;

  /* Borders */
  --border-width: 1px;
  --border-width-heavy: 2px;
  --radius-sm: 2px;
  --radius-md: 4px;
  --radius-lg: 6px;
  --radius-xl: 8px;

  /* Transitions */
  --ease-out: cubic-bezier(0.16, 1, 0.3, 1);
  --duration-fast: 150ms;
  --duration-normal: 200ms;
  --duration-slow: 300ms;

  /* Shadows — subtle, warm-toned, no glow */
  --shadow-sm: 0 1px 3px rgba(26, 23, 20, 0.08);
  --shadow-md: 0 2px 8px rgba(26, 23, 20, 0.10);
  --shadow-lg: 0 4px 16px rgba(26, 23, 20, 0.12);
}
```

**body and base element updates:**
- `body`: change `color` to `var(--text-primary)`, `background` to `var(--bg-base)`, `font-family` to `var(--font-body)`
- `a`: change `color` to `var(--accent-orange)`, `a:hover` to `var(--accent-orange-dark)`
- `button`: keep font-family as `var(--font-body)`
- `input, textarea, select`: keep font-family as `var(--font-body)`

**Remove these entire blocks (they are purely decorative or dark-theme-specific):**
- `.scanlines` and `.scanlines::after` blocks (lines 152–172)
- `.shadow-glow-gold` and `.shadow-glow-blue` from :root (already gone with new :root)
- `@keyframes float` block
- `@keyframes pulse-glow` block
- `@keyframes shimmer` block
- `.animate-float` class
- `.animate-pulse-glow` class
- `.animate-shimmer` class
- `.text-gold`, `.text-pink`, `.text-blue` classes (replace with `.text-orange { color: var(--accent-orange); }`)

**Replace .glass-panel and .glass-panel-heavy:**
```css
.surface-card {
  background: var(--bg-surface-raised);
  border: var(--border-width) solid var(--border-light);
  border-radius: var(--radius-md);
  box-shadow: var(--shadow-sm);
}

.surface-panel {
  background: var(--bg-surface);
  border: var(--border-width) solid var(--border-light);
  border-radius: var(--radius-md);
}
```

Keep `.glass-panel` and `.glass-panel-heavy` as aliases pointing to the same styles (for backward compatibility with JSX that still references them — we'll fix JSX in plan 02 and 03):
```css
.glass-panel { background: var(--bg-surface-raised); border: var(--border-width) solid var(--border-light); border-radius: var(--radius-md); box-shadow: var(--shadow-sm); }
.glass-panel-heavy { background: var(--bg-surface-raised); border: var(--border-width) solid var(--border-mid); border-radius: var(--radius-md); box-shadow: var(--shadow-md); }
```

**Replace .pixel-border:**
```css
.clean-border {
  border: var(--border-width) solid var(--border-light);
  border-radius: var(--radius-md);
}
/* backward compat alias */
.pixel-border { border: var(--border-width) solid var(--border-mid); border-radius: var(--radius-md); }
```

**Replace .btn-retro with .btn (TE x Rams style):**
```css
.btn {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  padding: var(--space-2) var(--space-4);
  font-family: var(--font-body);
  font-size: var(--text-sm);
  font-weight: 600;
  letter-spacing: 0.02em;
  color: #FFFFFF;
  background: var(--accent-orange);
  border: var(--border-width) solid var(--accent-orange);
  border-radius: var(--radius-md);
  box-shadow: none;
  transition: background var(--duration-fast) var(--ease-out),
              border-color var(--duration-fast) var(--ease-out),
              opacity var(--duration-fast) var(--ease-out);
  text-transform: none;
  user-select: none;
  cursor: pointer;
}

.btn:hover {
  background: var(--accent-orange-dark);
  border-color: var(--accent-orange-dark);
}

.btn:active {
  opacity: 0.85;
}

.btn:disabled {
  opacity: 0.4;
  cursor: not-allowed;
}

.btn--secondary {
  color: var(--text-primary);
  background: transparent;
  border-color: var(--border-mid);
}

.btn--secondary:hover {
  background: var(--bg-muted);
  border-color: var(--border-dark);
}

.btn--danger {
  background: var(--danger);
  border-color: var(--danger);
}

.btn--danger:hover {
  background: #B5301A;
  border-color: #B5301A;
}

/* Backward compat — .btn-retro maps to .btn */
.btn-retro {
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: var(--space-2);
  padding: var(--space-2) var(--space-4);
  font-family: var(--font-body);
  font-size: var(--text-sm);
  font-weight: 600;
  letter-spacing: 0.02em;
  color: #FFFFFF;
  background: var(--accent-orange);
  border: var(--border-width) solid var(--accent-orange);
  border-radius: var(--radius-md);
  box-shadow: none;
  transition: background var(--duration-fast) var(--ease-out),
              border-color var(--duration-fast) var(--ease-out);
  text-transform: none;
  user-select: none;
  cursor: pointer;
}

.btn-retro:hover { background: var(--accent-orange-dark); border-color: var(--accent-orange-dark); }
.btn-retro:active { opacity: 0.85; }
.btn-retro:disabled { opacity: 0.4; cursor: not-allowed; transform: none; }

.btn-retro--secondary {
  color: var(--text-primary);
  background: transparent;
  border-color: var(--border-mid);
  box-shadow: none;
}
.btn-retro--secondary:hover { background: var(--bg-muted); border-color: var(--border-dark); filter: none; }
.btn-retro--secondary:active { box-shadow: none; }

.btn-retro--orange { background: var(--accent-orange); border-color: var(--accent-orange); box-shadow: none; }
.btn-retro--orange:hover { background: var(--accent-orange-dark); box-shadow: none; }
.btn-retro--teal { background: var(--region-coast); border-color: var(--region-coast); box-shadow: none; }
.btn-retro--teal:hover { background: #2F6A85; box-shadow: none; }
.btn-retro--pink { background: var(--accent-orange); border-color: var(--accent-orange); box-shadow: none; }
.btn-retro--danger { background: var(--danger); border-color: var(--danger); box-shadow: none; }
.btn-retro--danger:hover { background: #B5301A; box-shadow: none; }

a.btn-retro { color: #FFFFFF; }
a.btn-retro:hover { color: #FFFFFF; }
a.btn-retro--secondary { color: var(--text-primary); }
a.btn-retro--secondary:hover { color: var(--text-primary); }
```

**Replace .input-retro:**
```css
.input-retro {
  width: 100%;
  padding: var(--space-2) var(--space-3);
  font-size: var(--text-base);
  font-family: var(--font-body);
  color: var(--text-primary);
  background: var(--bg-surface-raised);
  border: var(--border-width) solid var(--border-mid);
  border-radius: var(--radius-md);
  outline: none;
  transition: border-color var(--duration-fast) var(--ease-out),
              box-shadow var(--duration-fast) var(--ease-out);
}

.input-retro:focus {
  border-color: var(--accent-orange);
  box-shadow: 0 0 0 3px var(--accent-orange-bg);
}

.input-retro::placeholder {
  color: var(--text-dim);
}

textarea.input-retro {
  resize: vertical;
  min-height: 80px;
  line-height: 1.5;
}
```

**Replace .modal-overlay and .modal-content:**
```css
.modal-overlay {
  position: fixed;
  inset: 0;
  background: rgba(26, 23, 20, 0.4);
  backdrop-filter: blur(2px);
  display: flex;
  align-items: center;
  justify-content: center;
  z-index: 100;
  animation: fade-in var(--duration-fast) var(--ease-out);
  padding: var(--space-4);
}

.modal-content {
  width: 100%;
  max-width: 520px;
  max-height: 90vh;
  overflow-y: auto;
  padding: var(--space-8);
  animation: slide-up var(--duration-slow) var(--ease-out);
}
```

**Scrollbar — update to light theme:**
```css
::-webkit-scrollbar { width: 6px; height: 6px; }
::-webkit-scrollbar-track { background: var(--bg-surface); }
::-webkit-scrollbar-thumb { background: var(--border-mid); border-radius: 3px; }
::-webkit-scrollbar-thumb:hover { background: var(--border-dark); }
* { scrollbar-width: thin; scrollbar-color: var(--border-mid) var(--bg-surface); }
```

**Update .loading-dots:**
```css
.loading-dots span {
  display: inline-block;
  width: 5px;
  height: 5px;
  border-radius: 50%;
  background: var(--accent-orange);
  margin: 0 3px;
  animation: dot-pulse 1.4s ease-in-out infinite;
}
```

**Remove .dotted-divider and replace:**
```css
.divider {
  border: none;
  border-top: 1px solid var(--border-light);
  margin: var(--space-6) 0;
}
/* backward compat */
.dotted-divider { border: none; border-top: 1px solid var(--border-light); margin: var(--space-6) 0; }
```

**Update .mono-label:**
```css
.mono-label {
  font-family: var(--font-mono);
  font-size: var(--text-xs);
  letter-spacing: 0.10em;
  text-transform: uppercase;
  color: var(--text-muted);
}
```

Keep @keyframes fade-in, slide-up, slide-in-right, slide-in-left, dot-pulse, spin — these are functional animations used for UI behavior.

Keep responsive blocks — update font sizes to remain the same, update any referenced dark-mode specific variables to light equivalents.
  </action>

  <verify>
    <automated>grep -n "bg-base" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/index.css | head -5</automated>
    <automated>grep "F5F2ED" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/index.css</automated>
    <automated>grep "Daydream" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/index.css | wc -l</automated>
    <automated>grep "E8712B\|accent-orange" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/index.css | head -3</automated>
    <automated>grep "scanlines\|pulse-glow\|animate-shimmer\|shadow-glow" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/index.css | wc -l</automated>
    <automated>grep "Inter" /Users/michaellaufbahn/Documents/GitHub/Terrain/index.html</automated>
    <automated>grep "F5F2ED" /Users/michaellaufbahn/Documents/GitHub/Terrain/index.html</automated>
  </verify>

  <acceptance_criteria>
    - `grep "F5F2ED" src/index.css` returns at least 1 match (--bg-base assignment)
    - `grep "Daydream" src/index.css | wc -l` returns 0
    - `grep "scanlines\|pulse-glow\|shadow-glow" src/index.css | wc -l` returns 0
    - `grep "E8712B" src/index.css` returns at least 1 match (--accent-orange)
    - `grep "0D0A06\|1A1510" src/index.css | wc -l` returns 0 (no legacy dark colors)
    - `grep "Inter" index.html` returns the Google Fonts link with Inter
    - `grep "F5F2ED" index.html` returns the theme-color meta tag
    - `grep "Boogaloo\|Open.Sans\|Fredoka" index.html | wc -l` returns 0
    - `grep "btn-retro" src/index.css` returns at least 1 match (backward compat class remains)
    - `grep "glass-panel" src/index.css` returns at least 1 match (backward compat class remains)
    - `grep "input-retro" src/index.css` returns at least 1 match (backward compat class remains)
  </acceptance_criteria>

  <done>src/index.css contains the complete TE x Rams warm-neutral design token system. All dark palette variables replaced. Daydream font removed. Decorative animations (.scanlines, pulse-glow, shimmer, float) removed. Utility classes (btn-retro, glass-panel, input-retro) remapped to clean light-theme styles. index.html loads Inter only and uses #F5F2ED theme-color.</done>
</task>

</tasks>

<verification>
After execution, verify design system foundation:
1. `grep "0D0A06\|1A1510\|#3D2E\|bg-glass\|D4A853\|FF6B9D" src/index.css | wc -l` — should return 0 (all legacy dark tokens gone)
2. `grep "F5F2ED\|EFECE6\|E8712B" src/index.css | wc -l` — should return at least 5 (new tokens present)
3. `grep "Daydream\|Boogaloo\|Fredoka" src/index.css index.html | wc -l` — should return 0
4. `grep "@keyframes float\|@keyframes pulse-glow\|@keyframes shimmer" src/index.css | wc -l` — should return 0
</verification>

<success_criteria>
1. src/index.css: :root contains --bg-base: #F5F2ED, --accent-orange: #E8712B, --text-primary: #1A1714
2. src/index.css: No @font-face Daydream declaration
3. src/index.css: No .scanlines, no glow shadow variables, no gradient utility classes
4. src/index.css: .btn-retro, .glass-panel, .input-retro all render in the light warm theme (backward compat preserved)
5. index.html: Inter font loaded, theme-color is #F5F2ED
6. Any page opened in browser shows a warm off-white background instead of near-black
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-01-SUMMARY.md` documenting:
- Exact new token values committed
- Which legacy tokens were removed vs aliased
- Any decisions made during execution
</output>
