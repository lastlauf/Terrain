---
phase: 01-design-system
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/components/Navbar.jsx
  - src/components/CheckinModal.jsx
  - src/components/AddRegionModal.jsx
  - src/components/DashboardView.jsx
  - src/components/DispatchOverlay.jsx
  - src/components/FieldReport.jsx
  - src/components/TemplatesModal.jsx
  - src/components/ExportModal.jsx
  - src/components/Loader.jsx
autonomous: true
requirements:
  - DS-04
  - DS-05

must_haves:
  truths:
    - "Navbar renders on --bg-surface with --border-light bottom border — no dark glass or dark border"
    - "All modal headings use Inter font-weight 700, color --text-primary — no accent-gold, no Daydream"
    - "All modal containers use bg-surface-raised, border-light, shadow-md — no dark glass-panel"
    - "All buttons in components use the new .btn styles or equivalent inline values"
    - "No hardcoded hex values from the old palette (#0D0A06, #1A1510, #3D2E1A, #D4A853, #FF6B9D) remain in any file in this plan"
    - "DashboardView region rows render cleanly on light background — text-primary for names, border-light dividers"
  artifacts:
    - path: "src/components/Navbar.jsx"
      provides: "Top navigation bar with light theme"
      contains: "F5F2ED"
    - path: "src/components/CheckinModal.jsx"
      provides: "Check-in modal with light theme"
    - path: "src/components/DashboardView.jsx"
      provides: "Region list panel with light theme"
  key_links:
    - from: "src/components/Navbar.jsx"
      to: "src/index.css"
      via: "CSS custom properties in inline styles"
      pattern: "var\\(--bg"
    - from: "src/components/DashboardView.jsx"
      to: "src/index.css"
      via: "CSS custom properties"
      pattern: "var\\(--text-primary\\)"
---

<objective>
Retheme all core app components — Navbar, modals, and the DashboardView panel — to the TE x Rams warm neutral system. All hardcoded dark palette values are replaced with CSS custom property references from the tokens set up in plan 01. Logic is preserved entirely; only visual presentation changes.

Purpose: The main app shell (nav + dashboard + modals) is the majority of what users see. Getting these right means the app looks right even before page-level work in plan 03.
Output: 8 component files with fully updated visual styles. Zero remaining references to the old dark palette.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system/01-01-SUMMARY.md

@src/index.css
</context>

<interfaces>
<!-- Design tokens established in Plan 01 — use these exact variable names -->
/* Colors */
--bg-base: #F5F2ED
--bg-surface: #EFECE6
--bg-surface-raised: #FFFFFF
--bg-muted: #E8E4DC
--border-light: #DDD8CE
--border-mid: #C8C2B5
--border-dark: #A89E8E
--accent-orange: #E8712B
--accent-orange-dark: #C45A1A
--accent-orange-bg: rgba(232, 113, 43, 0.08)
--text-primary: #1A1714
--text-secondary: #4A4540
--text-muted: #7A7268
--text-dim: #A89E8E
--region-mountains: #4A6FA5
--region-forest: #4A8C5C
--region-city: #C45A1A
--region-coast: #3A7D9E
--danger: #D93B20

/* Shadows */
--shadow-sm: 0 1px 3px rgba(26, 23, 20, 0.08)
--shadow-md: 0 2px 8px rgba(26, 23, 20, 0.10)
--shadow-lg: 0 4px 16px rgba(26, 23, 20, 0.12)

/* Fonts — Inter only */
--font-display: 'Inter', sans-serif  (use font-weight: 700 or 800 for display)
--font-heading: 'Inter', sans-serif
--font-body: 'Inter', sans-serif
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Retheme Navbar and Loader</name>

  <read_first>
    - src/components/Navbar.jsx (read full file before any edits)
    - src/components/Loader.jsx (read full file before any edits)
  </read_first>

  <files>src/components/Navbar.jsx, src/components/Loader.jsx</files>

  <action>
**Navbar.jsx — inline style replacements:**

The nav element's background is currently `rgba(13, 10, 6, 0.85)`. Replace with:
```js
background: 'var(--bg-surface)',
```
Remove `backdropFilter` and `WebkitBackdropFilter` — no blur on light background.

The borderBottom is currently `'2px solid var(--border-retro)'`. Replace with:
```js
borderBottom: '1px solid var(--border-light)',
```

The logo Link currently uses `fontFamily: 'var(--font-display)'` and `color: 'var(--accent-gold)'`. Replace with:
```js
fontFamily: 'var(--font-heading)',
fontWeight: 800,
fontSize: 'var(--text-base)',
letterSpacing: '0.08em',
color: 'var(--text-primary)',
```

Nav links (Map, Explore) currently use `color: location.pathname === '/map' ? 'var(--accent-gold)' : 'var(--text-muted)'`. Replace with:
```js
color: location.pathname === '/map' ? 'var(--accent-orange)' : 'var(--text-muted)',
fontWeight: location.pathname === '/map' ? 600 : 400,
```
Same pattern for the Explore link.

The user menu button currently uses `background: 'var(--bg-glass)'` and `border: '1px solid var(--border-retro)'`. Replace with:
```js
background: 'transparent',
border: '1px solid var(--border-light)',
```

The avatar span (username initial) currently uses `background: 'var(--accent-gold)'` and `color: 'var(--bg-base)'`. Replace with:
```js
background: 'var(--accent-orange)',
color: '#FFFFFF',
borderRadius: '50%',
```

The dropdown menu currently uses `background: 'var(--bg-surface)'` and `border: '2px solid var(--border-retro)'`. Replace with:
```js
background: 'var(--bg-surface-raised)',
border: '1px solid var(--border-light)',
boxShadow: 'var(--shadow-md)',
```

Dropdown button hover handlers: replace `var(--bg-glass)` with `var(--bg-muted)`.

Remove the PixelPaletteIcon import and the onThemeToggle button entirely — ThemePanel is legacy. Keep only the user menu and nav links.

**Loader.jsx — retheme entirely:**
Read the file first. Replace any dark backgrounds with `var(--bg-base)`, any gold/accent colors with `var(--accent-orange)`, any dark text with `var(--text-primary)`. If it renders loading dots, the dots should use `var(--accent-orange)`.
  </action>

  <verify>
    <automated>grep "accent-gold\|bg-glass\|border-retro\|0D0A0\|rgba(13" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/Navbar.jsx | wc -l</automated>
    <automated>grep "accent-orange\|bg-surface\|border-light" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/Navbar.jsx | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep "accent-gold\|bg-glass\|border-retro\|0D0A06\|1A1510\|D4A853\|rgba(13" src/components/Navbar.jsx | wc -l` returns 0
    - `grep "accent-orange\|bg-surface\|text-primary" src/components/Navbar.jsx | wc -l` returns at least 4
    - `grep "backdropFilter\|WebkitBackdropFilter" src/components/Navbar.jsx | wc -l` returns 0
    - `grep "PixelPaletteIcon\|onThemeToggle" src/components/Navbar.jsx | wc -l` returns 0
  </acceptance_criteria>

  <done>Navbar renders on --bg-surface with --border-light bottom border. Logo is Inter 800 weight, text-primary color. Nav links use accent-orange for active state. User menu button is clean borderless. Avatar uses accent-orange. Dropdown menu has shadow-md, no dark backgrounds.</done>
</task>

<task type="auto">
  <name>Task 2: Retheme all modals and DashboardView</name>

  <read_first>
    - src/components/CheckinModal.jsx (read full file)
    - src/components/AddRegionModal.jsx (read full file)
    - src/components/DashboardView.jsx (read full file)
    - src/components/DispatchOverlay.jsx (read first 80 lines)
    - src/components/FieldReport.jsx (read first 80 lines)
    - src/components/TemplatesModal.jsx (read first 80 lines)
    - src/components/ExportModal.jsx (read first 80 lines)
  </read_first>

  <files>
    src/components/CheckinModal.jsx,
    src/components/AddRegionModal.jsx,
    src/components/DashboardView.jsx,
    src/components/DispatchOverlay.jsx,
    src/components/FieldReport.jsx,
    src/components/TemplatesModal.jsx,
    src/components/ExportModal.jsx
  </files>

  <action>
Apply these retheme rules consistently to every file listed. Read each file fully before editing.

**Universal substitution rules (apply to ALL files in this task):**

| Find (old value) | Replace with |
|---|---|
| `var(--accent-gold)` | `var(--accent-orange)` |
| `var(--accent-pink)` | `var(--accent-orange)` |
| `var(--accent-blue)` | `var(--region-mountains)` |
| `var(--bg-base)` | `var(--bg-base)` (keep — now light) |
| `var(--bg-surface)` | `var(--bg-surface)` (keep — now light) |
| `var(--bg-glass)` | `var(--bg-muted)` |
| `var(--bg-glass-heavy)` | `var(--accent-orange-bg)` |
| `var(--border-retro)` | `var(--border-mid)` |
| `var(--border-retro-light)` | `var(--border-light)` |
| `rgba(0, 0, 0, 0.3)` or `rgba(0,0,0,0.3)` | `rgba(26, 23, 20, 0.08)` |
| `rgba(0, 0, 0, 0.4)` or `rgba(0,0,0,0.4)` | `rgba(26, 23, 20, 0.10)` |
| `rgba(0, 0, 0, 0.7)` or `rgba(0,0,0,0.7)` | `rgba(26, 23, 20, 0.40)` |
| `var(--font-display)` | `var(--font-heading)` (remove any `fontWeight` set to 400 or below for headings — set to 700) |
| Hardcoded `#0D0A06` | `var(--bg-base)` |
| Hardcoded `#1A1510` | `var(--bg-surface)` |
| Hardcoded `#3D2E1A` or `#5A4530` | `var(--border-mid)` |
| Hardcoded `#D4A853` | `var(--accent-orange)` |
| Hardcoded `#FF6B9D` | `var(--accent-orange)` |
| Hardcoded `#4A90D9` | `var(--region-mountains)` |
| Hardcoded `#F5E6C8` | `var(--text-primary)` |
| Hardcoded `#8A7560` | `var(--text-muted)` |
| Hardcoded `#5A4A38` or `#5A4538` | `var(--text-dim)` |
| `var(--shadow-glow-gold)` | `none` |
| `var(--shadow-glow-blue)` | `none` |
| `border-radius: 0` | `border-radius: var(--radius-md)` |
| `box-shadow: 0 3px 0 rgba(0,0,0,0.3)` | `box-shadow: none` |
| `box-shadow: 0 4px 0 rgba(0,0,0,0.3)` | `box-shadow: none` |

**DashboardView.jsx specific changes:**

The panel container (root div) currently likely has dark background. Set to:
```js
background: 'var(--bg-surface)',
borderLeft: '1px solid var(--border-light)',
height: '100%',
overflowY: 'auto',
```

The header / title area: replace dark backgrounds with `var(--bg-surface)`, `border-bottom: '1px solid var(--border-light)'`.

Progress bar background: replace `rgba(0,0,0,0.4)` and `border: '1px solid var(--border-retro)'` with:
```js
background: 'var(--bg-muted)',
border: 'none',
```

Category color values in CATEGORY_COLORS constant: These are used in pills — update to new palette colors:
```js
const CATEGORY_COLORS = {
  physical: '#4A6FA5',
  creative: '#C45A1A',
  financial: '#4A8C5C',
  relationships: '#3A7D9E',
  learning: '#7A5AB5',
  other: '#7A7268',
}
```

COLOR_PRESETS in AddRegionModal.jsx — update to:
```js
const COLOR_PRESETS = [
  '#4A6FA5', '#4A8C5C', '#C45A1A', '#3A7D9E',
  '#3A5F8A', '#7A5AB5', '#4A8070', '#C47A3A',
]
```

**Heading styles in modals (CheckinModal, AddRegionModal, FieldReport, etc.):**
Any h2 or title element currently using `fontFamily: 'var(--font-display)'` and `color: 'var(--accent-gold)'`:
Replace with:
```js
fontFamily: 'var(--font-heading)',
fontWeight: 700,
fontSize: 'var(--text-xl)',
color: 'var(--text-primary)',
```

**Modal container/wrapper in CheckinModal.jsx:**
The `.modal-content.glass-panel-heavy` already gets remapped by Plan 01 CSS, but verify the inline styles on the containing divs don't hardcode dark values.

**DispatchOverlay.jsx:**
This is the "morning dispatch" letter overlay. It likely has a decorative dark style. Apply the universal substitution rules. The overlay background should become `rgba(26, 23, 20, 0.40)`. The content card should be `var(--bg-surface-raised)` with `var(--shadow-lg)`.

**TemplatesModal.jsx and ExportModal.jsx:**
Apply universal substitution rules. These are utility modals — make them clean and functional. Background: `var(--bg-surface-raised)`, borders: `var(--border-light)`, text: `var(--text-primary)` / `var(--text-secondary)`.
  </action>

  <verify>
    <automated>grep -r "accent-gold\|bg-glass\|border-retro\|D4A853\|FF6B9D\|0D0A06\|1A1510\|3D2E1A\|F5E6C8\|shadow-glow" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/CheckinModal.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/AddRegionModal.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/DashboardView.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/DispatchOverlay.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/FieldReport.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/TemplatesModal.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/ExportModal.jsx | wc -l</automated>
    <automated>grep -r "accent-orange\|text-primary\|border-light\|bg-surface" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/DashboardView.jsx | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - Running `grep "accent-gold\|bg-glass\|border-retro\|D4A853\|FF6B9D\|0D0A06\|1A1510\|3D2E1A\|shadow-glow" src/components/CheckinModal.jsx src/components/AddRegionModal.jsx src/components/DashboardView.jsx src/components/DispatchOverlay.jsx src/components/FieldReport.jsx src/components/TemplatesModal.jsx src/components/ExportModal.jsx | wc -l` returns 0
    - `grep "accent-orange" src/components/CheckinModal.jsx` returns at least 1 match
    - `grep "accent-orange" src/components/AddRegionModal.jsx` returns at least 1 match
    - `grep "text-primary\|bg-surface" src/components/DashboardView.jsx | wc -l` returns at least 5
    - `grep "border-radius: 0" src/components/CheckinModal.jsx src/components/AddRegionModal.jsx | wc -l` returns 0
    - `grep "font-display" src/components/CheckinModal.jsx src/components/AddRegionModal.jsx | wc -l` returns 0 (or only uses var(--font-heading) variant)
  </acceptance_criteria>

  <done>All 7 component files have zero references to the old dark palette. Modal headings use Inter weight 700 in text-primary. DashboardView region list renders cleanly on light background with border-light dividers and accent-orange accent color. All overlay backgrounds use the warm neutral modal overlay color. Progress bars use bg-muted track. Pills use updated category colors.</done>
</task>

</tasks>

<verification>
After both tasks complete, run across all files in this plan:
```
grep -r "accent-gold\|bg-glass\|border-retro\|D4A853\|FF6B9D\|0D0A06\|1A1510\|3D2E1A\|F5E6C8\|shadow-glow" \
  src/components/Navbar.jsx \
  src/components/Loader.jsx \
  src/components/CheckinModal.jsx \
  src/components/AddRegionModal.jsx \
  src/components/DashboardView.jsx \
  src/components/DispatchOverlay.jsx \
  src/components/FieldReport.jsx \
  src/components/TemplatesModal.jsx \
  src/components/ExportModal.jsx | wc -l
```
Must return 0.
</verification>

<success_criteria>
1. Navbar: warm surface background, Inter 800 logo, accent-orange active links, no dark glass
2. All modals: white/surface-raised backgrounds, Inter 700 headings, text-primary color, border-light borders
3. DashboardView: light background panel, clear typography hierarchy, accent-orange action buttons
4. Zero remaining dark palette hex values across all 8 component files
5. No gradient backgrounds, no glow shadows, no pixel/retro border-radius: 0 patterns (except where radius-sm applies)
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-02-SUMMARY.md` documenting:
- Which files were changed
- Any edge cases encountered in the substitution
- Any components that needed special handling beyond the substitution table
</output>
