---
phase: 01-design-system
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - src/pages/Landing.jsx
  - src/pages/Login.jsx
  - src/pages/Signup.jsx
  - src/pages/Onboarding.jsx
  - src/pages/Region.jsx
  - src/pages/Explore.jsx
  - src/components/SignupOverlay.jsx
  - src/components/OnboardingTour.jsx
  - src/components/LandmarkNaming.jsx
  - src/components/ThemePanel.jsx
autonomous: true
requirements:
  - DS-04
  - DS-05

must_haves:
  truths:
    - "Landing page renders on --bg-base (#F5F2ED) — no dark hero section or dark full-page background"
    - "Login and Signup forms use bg-surface-raised, border-light inputs, accent-orange CTA button"
    - "Region page uses light backgrounds throughout — no dark panels or dark canvas backdrop"
    - "Explore page uses light background — no dark card backgrounds"
    - "Onboarding uses light backgrounds and Inter font — no Daydream, no dark backgrounds"
    - "No hardcoded dark palette values (#0D0A06, #1A1510, D4A853, FF6B9D) remain in any file"
  artifacts:
    - path: "src/pages/Landing.jsx"
      provides: "Landing page with light theme"
    - path: "src/pages/Login.jsx"
      provides: "Login page with light theme"
    - path: "src/pages/Region.jsx"
      provides: "Region detail page with light theme"
    - path: "src/pages/Explore.jsx"
      provides: "Explore page with light theme"
  key_links:
    - from: "src/pages/Landing.jsx"
      to: "src/index.css"
      via: "CSS custom properties"
      pattern: "var\\(--bg-base\\)"
    - from: "src/pages/Login.jsx"
      to: "src/index.css"
      via: "CSS custom properties"
      pattern: "var\\(--accent-orange\\)"
---

<objective>
Retheme all page components and the remaining overlay/tour components to the TE x Rams warm neutral system. These are the user-facing screens — Landing, Login, Signup, Region, Explore, Onboarding — plus the supporting overlay components SignupOverlay, OnboardingTour, LandmarkNaming, and ThemePanel.

Purpose: After plans 01 and 02, the app shell and modals are light. This plan completes the retheme across every page and overlay — leaving no dark corner visible.
Output: 10 files fully rethemed. No dark palette values anywhere in the page layer.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-design-system/01-01-SUMMARY.md

@src/index.css
</context>

<interfaces>
<!-- Design tokens established in Plan 01 — use these exact variable names -->
/* Colors */
--bg-base: #F5F2ED
--bg-surface: #EFECE6
--bg-surface-raised: #FFFFFF
--bg-muted: #E8E4DC
--border-light: #DDD8CE
--border-mid: #C8C2B5
--border-dark: #A89E8E
--accent-orange: #E8712B
--accent-orange-dark: #C45A1A
--accent-orange-bg: rgba(232, 113, 43, 0.08)
--text-primary: #1A1714
--text-secondary: #4A4540
--text-muted: #7A7268
--text-dim: #A89E8E
--region-mountains: #4A6FA5
--region-forest: #4A8C5C
--region-city: #C45A1A
--region-coast: #3A7D9E
--danger: #D93B20

/* Fonts — Inter only */
--font-display: 'Inter', sans-serif  (use font-weight 700 or 800 for display headings)
--font-heading: 'Inter', sans-serif
--font-body: 'Inter', sans-serif
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Retheme Landing, Login, and Signup pages</name>

  <read_first>
    - src/pages/Landing.jsx (read first 200 lines to understand hero section structure)
    - src/pages/Login.jsx (read full file)
    - src/pages/Signup.jsx (read full file — may be short)
    - src/components/SignupOverlay.jsx (read full file)
  </read_first>

  <files>src/pages/Landing.jsx, src/pages/Login.jsx, src/pages/Signup.jsx, src/components/SignupOverlay.jsx</files>

  <action>
Apply the universal substitution rules from Plan 02 to all four files (same table of old → new values).

**Landing.jsx specific changes:**

The page root div currently likely sets `background: 'var(--bg-base)'` which is now #F5F2ED — that's correct. Verify and keep.

The hero section: likely has dark background or gradient. Replace any dark backgrounds with `var(--bg-base)`. Replace any gradient hero backgrounds with solid `var(--bg-base)`.

Headline text: Any `fontFamily: 'var(--font-display)'` for the main headline — replace with `fontFamily: 'var(--font-heading)', fontWeight: 800`. Keep `fontSize` as-is (likely var(--text-3xl) or larger).

The pixel art components (PixelMountain, PixelTree, PixelCity, PixelWave) defined at the top of Landing.jsx use hardcoded color arrays like `['transparent', '#F5E6C8', '#4A90D9', '#3A72B0']`. These are small canvas SVG illustrations — do NOT change their internal color arrays. They are illustrative elements, not UI chrome. Leave them as-is.

CTA buttons: apply `.btn` style — `background: 'var(--accent-orange)'`, `color: '#FFFFFF'`, `border: '1px solid var(--accent-orange)'`, `borderRadius: 'var(--radius-md)'`, `fontFamily: 'var(--font-body)'`, `fontWeight: 600`, no `linear-gradient`, no `textTransform: 'uppercase'` (remove), no `letterSpacing: '1px'`.

Secondary buttons (if any): `background: 'transparent'`, `color: 'var(--text-primary)'`, `border: '1px solid var(--border-mid)'`.

Feature cards / sections: replace `var(--bg-surface)` (now light) or dark backgrounds with `var(--bg-surface-raised)` for elevated cards, `var(--bg-surface)` for sections. Borders: `var(--border-light)`.

Footer: if dark, replace with `var(--bg-surface)`, `borderTop: '1px solid var(--border-light)'`, text `var(--text-muted)`.

Remove any `scanlines` className usages from Landing.jsx.

**Login.jsx specific changes:**

Page root: `background: 'var(--bg-base)'`, `minHeight: '100vh'`.

Form container: `background: 'var(--bg-surface-raised)'`, `border: '1px solid var(--border-light)'`, `borderRadius: 'var(--radius-lg)'`, `boxShadow: 'var(--shadow-md)'`.

Logo / title: `fontFamily: 'var(--font-heading)'`, `fontWeight: 800`, `color: 'var(--text-primary)'`.

Input fields: already `.input-retro` class which is remapped in Plan 01. Check inline styles too.

Primary button (Sign In): `background: 'var(--accent-orange)'`, `color: '#FFFFFF'`, `border: 'none'`, `borderRadius: 'var(--radius-md)'`, `fontWeight: 600`, no gradient.

Magic link button / secondary action: `color: 'var(--accent-orange)'`, no background.

Error message: `color: 'var(--danger)'`, `background: 'rgba(217, 59, 32, 0.08)'`, `border: '1px solid var(--danger)'`, `borderRadius: 'var(--radius-md)'`.

Remove PixelSparkleIcon import and usage if it references a pixel art icon component — replace with a plain text label or omit the decorative icon entirely.

Link to signup: `color: 'var(--accent-orange)'`.

**Signup.jsx:**
This may redirect to Landing or use SignupOverlay. Apply the same rules. If it's minimal, ensure the page background is `var(--bg-base)`.

**SignupOverlay.jsx:**
This is the signup modal/overlay. Apply universal substitution rules. The overlay container background: `rgba(26, 23, 20, 0.40)`. The form card: `var(--bg-surface-raised)`, `var(--shadow-lg)`, `border: '1px solid var(--border-light)'`. Form title: Inter 700, text-primary. All inputs: .input-retro class (already remapped). Submit button: accent-orange solid.
  </action>

  <verify>
    <automated>grep "accent-gold\|bg-glass\|border-retro\|D4A853\|FF6B9D\|0D0A06\|1A1510\|3D2E1A\|F5E6C8\|shadow-glow\|linear-gradient.*4A90\|linear-gradient.*FF6B" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/pages/Landing.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/pages/Login.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/SignupOverlay.jsx | wc -l</automated>
    <automated>grep "accent-orange\|bg-surface\|text-primary" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/pages/Login.jsx | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep "accent-gold\|bg-glass\|border-retro\|D4A853\|FF6B9D\|0D0A06\|1A1510\|3D2E1A\|shadow-glow" src/pages/Landing.jsx src/pages/Login.jsx src/pages/Signup.jsx src/components/SignupOverlay.jsx | wc -l` returns 0
    - `grep "bg-base\|bg-surface\|accent-orange" src/pages/Landing.jsx | wc -l` returns at least 3
    - `grep "accent-orange" src/pages/Login.jsx` returns at least 1 match (CTA button)
    - `grep "scanlines" src/pages/Landing.jsx | wc -l` returns 0
    - `grep "textTransform.*uppercase" src/pages/Login.jsx | wc -l` returns 0 (buttons not all-caps)
    - `grep "linear-gradient.*4A90\|linear-gradient.*FF6B\|linear-gradient.*D4A8" src/pages/Landing.jsx | wc -l` returns 0 (no old gradient buttons)
  </acceptance_criteria>

  <done>Landing page renders on warm off-white with no dark sections. CTA buttons are solid accent-orange, no gradient. Login form is clean white card on off-white background with accent-orange primary action. Signup overlay is clean white on warm overlay. No legacy dark palette values in any of the 4 files.</done>
</task>

<task type="auto">
  <name>Task 2: Retheme Region, Explore, Onboarding pages and tour overlays</name>

  <read_first>
    - src/pages/Region.jsx (read first 120 lines)
    - src/pages/Explore.jsx (read first 100 lines)
    - src/pages/Onboarding.jsx (read first 100 lines)
    - src/components/OnboardingTour.jsx (read first 80 lines)
    - src/components/LandmarkNaming.jsx (read full file — likely short)
    - src/components/ThemePanel.jsx (read first 80 lines)
  </read_first>

  <files>
    src/pages/Region.jsx,
    src/pages/Explore.jsx,
    src/pages/Onboarding.jsx,
    src/components/OnboardingTour.jsx,
    src/components/LandmarkNaming.jsx,
    src/components/ThemePanel.jsx
  </files>

  <action>
Apply the universal substitution rules from Plan 02 to all six files.

**Region.jsx specific changes:**

Page background: `var(--bg-base)`.

The region header/hero area (likely shows region name, type, stats): Replace dark card backgrounds with `var(--bg-surface)`. Use `borderBottom: '1px solid var(--border-light)'` for section separators. Region name: Inter 700, text-primary. Region type/category pills: use the updated CATEGORY_COLORS approach (lighter, muted versions on accent tint backgrounds):
```js
// example pill style
{
  fontSize: 'var(--text-xs)',
  fontWeight: 600,
  color: 'var(--region-mountains)', // or appropriate region/category color
  background: 'var(--accent-orange-bg)',
  borderRadius: 'var(--radius-sm)',
  padding: '2px var(--space-2)',
  letterSpacing: '0.06em',
  textTransform: 'uppercase',
}
```

Stats/metrics cards: `background: 'var(--bg-surface-raised)'`, `border: '1px solid var(--border-light)'`, `borderRadius: 'var(--radius-md)'`.

Checkin history list: `borderBottom: '1px solid var(--border-light)'` between rows. Text: text-primary for content, text-muted for metadata.

Progress bar: track `var(--bg-muted)`, fill `var(--accent-orange)`.

**Explore.jsx specific changes:**

Page/grid background: `var(--bg-base)`.

Region cards (if any): `background: 'var(--bg-surface-raised)'`, `border: '1px solid var(--border-light)'`, `borderRadius: 'var(--radius-md)'`, `boxShadow: 'var(--shadow-sm)'`, `boxShadow` on hover: `var(--shadow-md)`.

Section headers: Inter 600 or 700, text-primary.

Search/filter inputs if present: use .input-retro class styling.

**Onboarding.jsx specific changes:**

Step backgrounds: `var(--bg-base)`. Step cards/panels: `var(--bg-surface-raised)`.

Step titles: Inter 700, text-primary (large size — var(--text-2xl) or var(--text-xl)).

Step descriptions: Inter 400, text-secondary, var(--text-base).

Progress indicators (dots or steps): active dot/step: `var(--accent-orange)`, inactive: `var(--border-mid)`.

CTA button: standard accent-orange solid button.

**OnboardingTour.jsx specific changes:**

This is likely a tooltip-based tour. Tooltip boxes: `background: 'var(--bg-surface-raised)'`, `border: '1px solid var(--border-mid)'`, `boxShadow: 'var(--shadow-lg)'`. Tooltip title: Inter 700, text-primary. Tooltip body: text-secondary.

Highlight ring / spotlight: if using dark overlay, change to `rgba(26, 23, 20, 0.35)`.

Next/Skip buttons in tour: Next → accent-orange, Skip → text-muted with no background.

**LandmarkNaming.jsx:**
This is a naming UI component. Apply universal substitution rules. Keep it minimal — clean input on bg-surface-raised, button in accent-orange.

**ThemePanel.jsx:**
This entire component manages the old theme system (which is being superseded by the new design system). Rather than maintaining it, make it minimal or effectively inert:
- Apply universal substitution rules so it doesn't render dark-on-dark
- Change its container to bg-surface-raised, border-light
- It will be removed/replaced in a future phase — for now ensure it doesn't look broken
- If it has a color picker for accent colors, the default/current selection should be accent-orange (#E8712B)
  </action>

  <verify>
    <automated>grep "accent-gold\|bg-glass\|border-retro\|D4A853\|FF6B9D\|0D0A06\|1A1510\|3D2E1A\|F5E6C8\|shadow-glow" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/pages/Region.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/pages/Explore.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/pages/Onboarding.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/OnboardingTour.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/LandmarkNaming.jsx /Users/michaellaufbahn/Documents/GitHub/Terrain/src/components/ThemePanel.jsx | wc -l</automated>
    <automated>grep "accent-orange\|bg-surface\|text-primary" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/pages/Region.jsx | wc -l</automated>
    <automated>grep "accent-orange" /Users/michaellaufbahn/Documents/GitHub/Terrain/src/pages/Onboarding.jsx | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `grep "accent-gold\|bg-glass\|border-retro\|D4A853\|FF6B9D\|0D0A06\|1A1510\|3D2E1A\|shadow-glow" src/pages/Region.jsx src/pages/Explore.jsx src/pages/Onboarding.jsx src/components/OnboardingTour.jsx src/components/LandmarkNaming.jsx src/components/ThemePanel.jsx | wc -l` returns 0
    - `grep "accent-orange\|bg-surface\|text-primary" src/pages/Region.jsx | wc -l` returns at least 3
    - `grep "accent-orange" src/pages/Onboarding.jsx | wc -l` returns at least 1
    - `grep "font-display\|Daydream" src/pages/Onboarding.jsx | wc -l` returns 0 (no pixel font)
    - `grep "border-radius: 0" src/pages/Region.jsx src/pages/Explore.jsx | wc -l` returns 0
  </acceptance_criteria>

  <done>All 6 files rendered on warm off-white backgrounds with Inter typography. Region page shows clean stats cards and checkin history on light surfaces. Explore renders region cards on bg-surface-raised with border-light. Onboarding uses accent-orange progress indicator and CTA. No dark palette values remain in any of the 6 files.</done>
</task>

</tasks>

<verification>
Full phase check — run across all page files:
```
grep -r "accent-gold\|bg-glass\|border-retro\|D4A853\|FF6B9D\|0D0A06\|1A1510\|3D2E1A\|F5E6C8\|shadow-glow" \
  src/pages/Landing.jsx \
  src/pages/Login.jsx \
  src/pages/Signup.jsx \
  src/pages/Onboarding.jsx \
  src/pages/Region.jsx \
  src/pages/Explore.jsx \
  src/components/SignupOverlay.jsx \
  src/components/OnboardingTour.jsx \
  src/components/LandmarkNaming.jsx \
  src/components/ThemePanel.jsx | wc -l
```
Must return 0.
</verification>

<success_criteria>
1. Every page renders on --bg-base (#F5F2ED) or --bg-surface — no dark backgrounds on any page
2. All headings use Inter font-weight 700 or 800, color --text-primary
3. All primary actions use --accent-orange solid buttons with no gradient
4. No decorative scanlines, glow shadows, or pixel-art borders on any page
5. Login/Signup forms are clean white cards on off-white — accessible and functional
6. Region page stats and checkin history read clearly on light surfaces
</success_criteria>

<output>
After completion, create `.planning/phases/01-design-system/01-03-SUMMARY.md` documenting:
- Which files were changed
- Any pixel art components intentionally left unchanged (and why)
- Any components that needed structural changes beyond style substitution
</output>
